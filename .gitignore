# Copyright (C) 2025-2026 Robin L. M. Cheung, MBA. All rights reserved.

# === TRULY SENSITIVE - NEVER TRACK ===
# SSL certificates and private keys
*.key
*.pem (for certificates)
*.crt
*.cer
infrastructure/nginx/ssl/

# Secrets and credentials
.env
.env.*
*.secret
*_password*
credentials.json
service-account.json

# === BUILD ARTIFACTS (generated, too large for GitHub) ===
# Downloads - APK, EXE, MSI, AppImage, DMG, DEB, RPM
downloads/*.apk
downloads/*.exe
downloads/*.msi
downloads/*.AppImage
downloads/*.dmg
downloads/*.deb
downloads/*.rpm
downloads/*.7z
downloads/*.zip

# Also in domains folder
domains/*/downloads/*.apk
domains/*/downloads/*.exe
domains/*/downloads/*.msi
domains/*/downloads/*.AppImage
domains/*/downloads/*.dmg
domains/*/downloads/*.deb
domains/*/downloads/*.rpm

# WASM files (build outputs)
*.wasm

# Node modules and dist
node_modules/
dist/

# === TEMPORARY/GENERATED FILES ===
# OS files
.DS_Store
Thumbs.db

# Editor swap files
*.swp
*~

# Logs
*.log

# Temporary files
*.tmp
*.temp

# Backup files
*.bak
*.out_of_sync
index.html.bak
index.html.out_of_sync

# === SSHFS MOUNT POINTS (not real files) ===
HELLOWORD.ROBIN.MBA/
SANCTISSIMISSA.ONLINE/

# === DO NOT IGNORE MEDIA CONTENT ===
# Media files (mp4, m4a, png, jpg, pdf) are CONTENT, not build artifacts
# These should be tracked for complete restore capability
downloads/
2026-*-local-command-*.txt
